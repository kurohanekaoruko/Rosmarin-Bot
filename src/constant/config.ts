import {RESOURCE_ABBREVIATIONS} from './ResourceConstant'

/**
 * 基础配置
 */
export const BaseConfig = {
    // bot名称
    BOT_NAME: 'Rosmarin',
    // 长名资源缩写
    RESOURCE_ABBREVIATIONS,
    // 过道观察间隔
    LookInterval: 10,
    // 沉积物最大冷却
    DepositMaxCooldown: 150,
    // 最小power数量限制
    PowerMinAmount: 1000,
}
